# coinTrackerApp
Demo app for CodeHub iOS reskilling training

## API key setup
The app reads the CoinGecko API key from either:
- `COINGECKO_API_KEY` environment variable
- `COINGECKO_API_KEY` key in `Info.plist`
